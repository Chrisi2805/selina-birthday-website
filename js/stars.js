(function(){const canvas=document.getElementById('star-canvas');if(!canvas)return;const ctx=canvas.getContext('2d',{alpha:true});let width=0,height=0,dpr=1,stars=[];const mobile=()=>window.innerWidth<700;function resize(){dpr=Math.min(window.devicePixelRatio||1,mobile()?1:1.5);width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+'px';canvas.style.height=height+'px';ctx.setTransform(dpr,0,0,dpr,0,0)}addEventListener('resize',resize,{passive:true});resize();function makeStars(){const count=mobile()?70:150;stars=Array.from({length:count},()=>({x:Math.random()*width,y:Math.random()*height,r:Math.random()*1.3,a:Math.random(),s:.005+Math.random()*.015}))}makeStars();let last=0;function draw(t){const interval=mobile()?50:20;if(t-last<interval){requestAnimationFrame(draw);return}last=t;ctx.clearRect(0,0,width,height);for(const s of stars){s.a+=s.s;if(s.a>1||s.a<0)s.s=-s.s;s.y-=mobile()?.08:.2;if(s.y<0)s.y=height;ctx.globalAlpha=Math.abs(s.a);ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fillStyle='#fff';ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(draw)}requestAnimationFrame(draw);window.starfield={getSize:()=>({width,height})}})();